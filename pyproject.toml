[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "pylzx"
version = "0.1.0"
description = "Rust-backed LZXD decoder for Python"
readme = "README.md"
requires-python = ">=3.8"

[tool.maturin]
module-name = "pylzx"
